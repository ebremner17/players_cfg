<h2 style="text-align: center">Upcoming Games</h2>
<div class="players-reserve-block">
  {% if games.future_games | length > 0 %}
    {% for game_info in games.future_games %}
      {% include '@players_theme/templates/content/game.html.twig' with {
        'game_day': game_info.game_day,
        'open_time': game_info.open_time,
        'games': game_info.games,
        'info': game_info.info,
        'food': game_info.food,
        'floor': game_info.floor,
        'dealers': game_info.dealers,
        'date': game_info.date,
        'current': game_info.current
      } %}
    {% endfor %}
  {% endif %}
  {% if games.tourneys | length > 0 %}
    <h2>Tournaments</h2>
    <div class="players-reserve-tourney">
      {% for tourney in games.tourneys %}
        <h3>{{ tourney.display_date }}</h3>
        <h4>{{ tourney.title }}: {{ tourney.start_time }} - {{ tourney.end_time }}</h4>
        <a href="/reserve/{{ tourney.date }}">
          <button class="players-button players-button--blue players-button--small">Reserve</button>
        </a>
      {% endfor %}

    </div>
  {% endif %}
</div>
